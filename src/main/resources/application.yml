server:
  port: 8002

spring:
  application:
    name: library-app


  datasource:
    url: jdbc:mysql://localhost:3306/demo_springboot_caching?useSSL=false
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

